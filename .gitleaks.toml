[extend]
useDefault = true

[[allowlists]]
description = """
docker/garage.toml はローカル開発 / CI 用 Garage の設定。
rpc_secret・admin_token・metrics_token は全て公開前提の
固定ダミー値であり、実環境の認証情報ではない。
"""
paths = ['''^docker/garage\.toml$''']
